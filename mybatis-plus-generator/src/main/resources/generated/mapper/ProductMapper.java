package com.example.mpgen.generated.mapper;

import com.baomidou.mybatisplus.core.mapper.BaseMapper;
import com.example.mpgen.generated.entity.Product;
import org.apache.ibatis.annotations.Mapper;

/**
 * <p>
 * Product Mapper - Generated by MyBatis-Plus
 * </p>
 *
 * @author mybatis-plus-generator
 * @since 2026-09-23
 */
@Mapper
public interface ProductMapper extends BaseMapper<Product> {

    // BaseMapper provides all basic CRUD operations:
    // - int insert(T entity)
    // - int deleteById(Serializable id)
    // - int updateById(T entity)
    // - T selectById(Serializable id)
    // - IPage<T> selectPage(IPage<T> page, Wrapper<T> queryWrapper)
    // - List<T> selectList(Wrapper<T> queryWrapper)
    // - int selectCount(Wrapper<T> queryWrapper)

    // Add custom SQL methods here if needed:
    // @Select("SELECT * FROM t_product WHERE category = #{category} AND deleted = 0")
    // List<Product> findByCategory(@Param("category") String category);
}
