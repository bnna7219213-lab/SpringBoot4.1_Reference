<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <title>Todo Detail - Mustache</title>
    <style>
        body { font-family: 'Segoe UI', Tahoma, Geneva, Verdana, sans-serif; max-width: 800px; margin: 0 auto; padding: 20px; background: #f5f7fa; }
        .breadcrumb { color: #64748b; font-size: 14px; margin-bottom: 16px; }
        .breadcrumb a { color: #4facfe; text-decoration: none; }
        .card { background: white; border-radius: 12px; padding: 32px; box-shadow: 0 2px 8px rgba(0,0,0,0.06); }
        .divider { height: 1px; background: #e2e8f0; margin: 24px 0; }
        .label { font-size: 12px; color: #64748b; text-transform: uppercase; font-weight: 600; }
        .value { font-size: 16px; color: #1e293b; }
        .grid { display: grid; grid-template-columns: 1fr 1fr; gap: 24px; }
        .status-badge { display: inline-block; padding: 6px 14px; border-radius: 20px; font-size: 13px; font-weight: 600; }
        .back-link { display: inline-block; margin-top: 24px; color: #4facfe; text-decoration: none; font-weight: 500; }
    </style>
</head>
<body>

<div class="breadcrumb">
    <a href="/todos">&#8592; Back to List</a> / <span>#{{todo.id}}</span>
</div>

<div class="card">
    <h1 style="margin: 0 0 8px; color: #1e293b;">{{todo.title}}</h1>

    <!-- Pre-computed badge values from Java service -->
    <div style="margin-bottom: 20px;">
        <span class="status-badge" style="background:{{todo.statusBgColor}};color:{{todo.statusTextColor}};">
            {{todo.statusIcon}} {{todo.statusText}}
        </span>
    </div>

    <div class="label">Description</div>
    <p style="margin: 0 0 24px; color: #475569; line-height: 1.6;">{{{todo.description}}}</p>

    <div class="divider"></div>

    <div class="grid">
        <div>
            <div class="label">Priority</div>
            <div class="value">{{todo.priority}}</div>
        </div>
        <div>
            <div class="label">Status</div>
            <div class="value">{{todo.statusText}}</div>
        </div>
        <div>
            <div class="label">Created</div>
            <div class="value">{{todo.formattedCreatedAt}}</div>
        </div>
        <div>
            <div class="label">Updated</div>
            <div class="value">{{todo.formattedUpdatedAt}}</div>
        </div>
    </div>

    <div class="divider"></div>

    <a href="/todos" class="back-link">&#8592; Back to List</a>
</div>

</body>
</html>
